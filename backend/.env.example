# 管理后台 Token（/admin 与 /api/v1/stats 使用）
ADMIN_TOKEN=请换成随机长字符串

# 允许的前端来源（逗号分隔）
ALLOWED_ORIGINS=https://luoyedashi.github.io,http://localhost:8080

# SQLite 路径（Render 等可挂载持久盘到 /data）
DATABASE_PATH=/data/guide.db
